#include<bits/stdc++.h>
using namespace std;
int main() {
    
    int n;
    cin >> n;

    // Complexity O(logN)
    for(int i = 1; i <= n; i*=2) {
        cout << i << endl;
    }

    // Complexity O(logN)

    return 0;
}

// Complexity O(logN)