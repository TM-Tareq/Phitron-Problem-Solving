#include<bits/stdc++.h>
using namespace std;
int main() {
    
    int n;
    cin >> n;
    // int a[n];

    // with declaring the size
    vector<int> v(n);

    kw

    for(int i = 0; i < n; i++) {
        cout << v[i];
    }

    // without declaring the size
    vector<int> v1;

    for(int i =0; i < n; i++) {
        int x;
        cin >> x;
        v.push_back(x);
    }

    return 0;
}