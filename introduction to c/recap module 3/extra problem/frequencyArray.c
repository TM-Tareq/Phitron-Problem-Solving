#include<stdio.h>
int main() {
    int N, M;

    scanf("%d", &N);
    int A[N];

    for(int i = 0; i < N; i++) {
        scanf("%d", &A[i]);
    }

    // applying from 1 to M condition
    for(int i = 1; i <= M; i++) {
        
    }
}