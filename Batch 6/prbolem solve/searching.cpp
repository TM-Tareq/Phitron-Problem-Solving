#include<bits/stdc++.h>
using namespace std;
void searching(long long int array[], int n, long long int key) {
    array[n];
    for(int i = 0; i < n; i++) {
        cin >> array[i];
    }
}

int main() {
    
    int n;
    cin >> n;

    long long int a[n];

    long long int key;
    cin >> key;

    searching(a, n, key);

    return 0;
}