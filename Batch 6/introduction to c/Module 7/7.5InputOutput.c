#include <stdio.h>
int main()
{
    int a[5];
    // // for normal variable input
    // scanf("%d%d", &a&b);
    // // for array variable input
    // scanf("%d", &a[0]);

    // using loop with the help of loop
    for (int i = 0; i < 5; i++)
    {
        scanf("%d", &a[i]);
    }
}