#include<bits/stdc++.h>
using namespace std;
int main() {
    
    int testCase;
    cin >> testCase;

    while(testCase --) {
        int n, m, r, c;

        cin >> n >> m >> r >> c;

        for(int i = 0; i < n * m; i++) {
            
        }
    }

    return 0;
}