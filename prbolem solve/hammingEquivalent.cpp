#include<bits/stdc++.h>
using namespace std;
int main() {
    
    int testCase;

    while(testCase --) {
        int length;
        cin >> length;

        int p[length];

        for(int i = 0; i < length; i++) {
            cin >> p[i];
        }

        for(int i = 0; i < length; i++) {
            if()
        }

    }

    return 0;
}